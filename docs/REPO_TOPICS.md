# Repository topics and description

This document records the canonical GitHub repo description and topics for `mcp-sql-bridge`.

---

## Repository description

> Read-only MCP server for SQL — give your AI assistant schema-aware access to SQLite, PostgreSQL, and MySQL. No cloud, no writes.

## Topics (GitHub repo tags)

```
mcp
model-context-protocol
sql
sqlite
postgresql
mysql
ai-tools
cursor
claude
read-only
database
python
fastapi
open-source
```

## How to set them via `gh` CLI

```bash
gh repo edit firas-mcp-servers/mcp-sql-bridge \
  --description "Read-only MCP server for SQL — give your AI assistant schema-aware access to SQLite, PostgreSQL, and MySQL. No cloud, no writes." \
  --add-topic mcp \
  --add-topic model-context-protocol \
  --add-topic sql \
  --add-topic sqlite \
  --add-topic postgresql \
  --add-topic mysql \
  --add-topic ai-tools \
  --add-topic cursor \
  --add-topic claude \
  --add-topic read-only \
  --add-topic database \
  --add-topic python \
  --add-topic fastapi \
  --add-topic open-source
```
